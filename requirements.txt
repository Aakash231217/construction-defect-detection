# Runtime requirements for the hosted Streamlit app.
# Detection is a direct HTTPS call to Roboflow (via requests) and the AI engine,
# so there is no OpenCV / torch / GPU dependency — installs on any Python version.
# For local training / report generation, install requirements-dev.txt instead.
streamlit>=1.35.0
pillow>=10.0.0
pandas>=2.2.0
requests>=2.31.0
python-dotenv>=1.0.1
openai>=1.0.0

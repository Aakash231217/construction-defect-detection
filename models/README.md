# Models

Place trained YOLO weights here after training, for example:

- `models/best.pt`

The training script starts from an open-source Ultralytics checkpoint such as `yolo11n.pt` or `yolov8n.pt`. That checkpoint is downloaded automatically by Ultralytics when training starts.
